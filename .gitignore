# Dependencies — never commit these
node_modules/

# Build output
dist/
dist-ssr/

# Local env files
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
Thumbs.db

# Supabase CLI scratch — link state, generated types, local containers
supabase/.temp/
supabase/.branches/

# Example env files are committed; real ones never are. The service role key
# belongs in Supabase secrets and Vercel env vars, and nowhere in this repo.
.env.example.local
